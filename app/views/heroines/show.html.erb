<h1><%= @heroine.name %></h1>
<h2><%= @heroine.super_name %></h2>

<ul>
<% @heroine.powers.each do |power| %>
    <li>
        <%= link_to power.name, power_path(power) %>
        <%= HeroinePower.where(heroine_id: @heroine.id).find_by(power_id: power.id).strength %>
    <li>
<% end %>
</ul>